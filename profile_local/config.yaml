# Local execution profile (macOS / no SLURM / no Singularity)
executor: local
logger: rich
cores: 4
configfile:
  - config/config.yaml
  - config/config_test.yaml
default-resources:
  mem_mb: 4000
  runtime: 120
